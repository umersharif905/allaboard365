# Feature — {short title}

**Status:** ⏳ pending | 🚧 in-progress | ✅ implemented | ✔️ verified end-to-end

## What it is

One-to-three sentence description of the feature.

## Why it's being added

Tied to a problem in `../current-system-problems.md` and/or a design decision in `../design.md`.

## Database impact

- New tables / columns / indexes / constraints
- Migration script path: `sql-changes/allaboard365/YYYY-MM-DD-*.sql`
- Default values, backfill rules, FK targets

## Backend impact

- New or modified route files (path + endpoint method/URL)
- New or modified service files
- Middleware touched
- Tests added/modified

## Frontend impact

- New or modified pages / components / hooks
- Where it surfaces in the UI (which page, which tab, which user role)
- Loading / empty / error states

## Use case

Concrete walkthrough: "A care team member at Tenant X opens the forms tab, clicks Y, sees Z."

## Test procedure

Steps to verify end-to-end after implementation. Each step should be a concrete action a human can perform.

1. Step one
2. Step two
3. Expected outcome

## Notes

Anything else — open questions, dependencies, follow-ups.
