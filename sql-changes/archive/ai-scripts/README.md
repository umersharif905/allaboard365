# Archived one-off SQL

Scripts moved from `ai_scripts/` for housekeeping: audits, backfills, data fixes, and local tooling. They are **not** a substitute for dated migrations in `sql-changes/`—review before running in any environment.
