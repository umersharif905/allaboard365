# imported-sql

DDL / schema SQL moved from **sharewell-csv-processor** (and similar) that we keep in the repo archive.

SQL that originated under **Project Docs** lives in `sql-dump/imported-sql/` (local-only) instead.
