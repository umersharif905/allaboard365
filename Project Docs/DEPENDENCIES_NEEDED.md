# Add these dependencies to package.json if not already present:

npm install react-hot-toast @hookform/resolvers zod

# Or add to your package.json:
"dependencies": {
  "react-hot-toast": "^2.4.1",
  "@hookform/resolvers": "^3.3.1",
  "zod": "^3.22.4"
}
