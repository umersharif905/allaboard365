/**
 * Default enrollment link expiration in hours.
 * Used across enrollment links, group onboarding links, etc.
 */
const DEFAULT_LINK_EXPIRATION_HOURS = 168; // 7 days

module.exports = { DEFAULT_LINK_EXPIRATION_HOURS };
